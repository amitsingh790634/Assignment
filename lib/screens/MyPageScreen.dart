import 'package:assignment/core/constants/allExport.dart';

class MyPageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("My Page Screen", style: TextStyle(fontSize: 24)));
  }
}
